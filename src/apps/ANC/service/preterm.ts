
//import { ProfileService } from "@/services/anc_profile_service"

export class Preterms {
  // async onSubmit(patientID: any,providerID: any, pretermData: any){
  //   const pretermService = new ProfileService(patientID, providerID)
  //   await pretermService.createEncounter()
  //   await pretermService.saveObservationList(pretermData)

  // }

}